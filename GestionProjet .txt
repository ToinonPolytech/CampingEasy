Problématiques futures et questions existentielles : 



- Faire un menu déroulant avec : - 100 valeurs (ageMin et ageMax )
								- Des valeurs de la base données (tableau avec les lieux par exemple) => for (...) { echo "<option value='$i'>".$i."</option>"; }
while ($data=$request->fetch()) { }
for ($i=0;$i<100;$i++)
								





User : 

- Besoin d'explications sur la gestion générale des users (controller formulaire et objet et modele )
	Q pour user : -access_level = admin / acceuil / client ... ? passage en formulaire lors de la création d'un user ? 
					- création user avec son id ? même problème que pour activité ? 
					- je ne me souviens plus comment on gère l'héritage pour la création d'un nouvel objet (appel dans le controller de la bonne class epour création admin ou client)



- Problème du dynamique : quand on recharge une page on se retrouve sur l'acceuil et non sur la page où on était ... 
	Facilement gérable, on stock en cookie, session ou je ne sais quoi le lieu où il était via le chargement dynamique et quand on est à l'accueil on regarde où il était


- Problème avec équipe : pas de score de départ donc n'existe pas dans la création du formulaire : soit on fait un formulaire différent pour la création et la modification soit on le passe en hidden 
	Oui mais quand y'a la création coté controller tu mets 0 de base





TODO (les petites choses à ne pas oublier) :

-gérer les formats date et strtotime (a faire surtout dans activité )
- ajouter l'expression régulière pour le format du numéro de téléphone du mail et  du site web du partenaire dans les deux controller du partenaire 
- A vérifier : tous les require 
- modifier tous les controller pour que l'objet fonctionne 
- page d'ajout user 
- Gérer les authentifications et l'affichage des pages en fonctions ainsi que la récupération des ID pour les objets en ayant besoin (équipe, activité .. )



	Ajout DB : 
		- Rajouter gestion des photos problème technique 
		-Ajout d'un type user partenaire 

TODO Lointain (améliorations possibles et détails dans un futur lointain ): 

- alerte pour création d'une activité dans plus d'un an 
- administration d'une activité partenaire par un admin 
- gestion des langues 
- gérer une attribution des points de l'activité avec classement (l'activité vaut 100 pour premier, 50 pour deuxieme etc ...) 
